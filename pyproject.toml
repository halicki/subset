[project]
name = "subset"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "pandera[polars]>=0.26.1",
]

[dependency-groups]
dev = [
    "ruff>=0.12.11",
    "ty>=0.0.1a20",
]
